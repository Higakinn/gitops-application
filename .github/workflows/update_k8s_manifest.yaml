name: GitOps-Demo CI

on:
  push:
    branches:
      - release/**
  pull_request:
    branches:
      - develop
      - main
    types: [closed]

env: # global環境変数を設定
  CR_NAME: ${{ secrets.CONTAINER_REGISTORY_NAME }}
  CR_USER: ${{ secrets.CONTAINER_REGISTORY_USER }}
  CR_PASS: ${{ secrets.CR_PAT }}
  IMAGE_NAME: ${{ secrets.IMAGE_NAME }}
  GH_WORKDIR: manifest
  GH_REPOSITORY: ${{ secrets.GH_REPOSITORY }}

jobs:
  push_docker_image:
    if: contains( github.ref, 'release') #releaseブランチにpushされたときに実行する
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout current branch
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      - name: Build docker image
        run: docker build -t $IMAGE_NAME -f ./docker/Dockerfile .
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ${{ env.CR_NAME }}
          username: ${{ env.CR_USER }}
          password: ${{ env.CR_PASS }}
      - name: Push docker image to Container Registry
        id: dockerimagetag
        run: |
          IMAGE_TAG=$(echo ${{ github.ref }} | sed -e "s#refs/heads/release/##g")
          IMAGE_NAME_WITH_CR="${CR_NAME}/${CR_USER}/${IMAGE_NAME}:${IMAGE_TAG}"
          echo $IMAGE_NAME_WITH_CR
          docker tag $IMAGE_NAME "${IMAGE_NAME_WITH_CR}"
          docker push "${IMAGE_NAME_WITH_CR}"
  open_pull_request_to_k8s_manifest_repo:
    if: contains( github.ref, 'release') #developブランチにpushされたときに実行する
    runs-on: ubuntu-latest
    needs: push_docker_image
    defaults:
      run:
        working-directory: ${{ env.GH_WORKDIR }}
    steps:
      - name: Checkout Manifest Repository
        uses: actions/checkout@v2
        with:
          repository: ${{ env.GH_REPOSITORY }}
          path: ${{ env.GH_WORKDIR }}
          token: ${{ secrets.PAT }}
      - name: SetUp Git
        id: config
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"
          git remote set-url origin "https://github.com/${GH_REPOSITORY}.git"
          git fetch
          IMAGE_TAG=$(echo ${{ github.ref }} | sed -e "s#refs/heads/release/##g")
          git checkout -b test/${IMAGE_TAG} origin/main || git checkout -b test/${IMAGE_TAG}
      - name: Update K8s Manifest
        run: |
          # yq コマンドのinstall
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod a+x /usr/local/bin/yq
          IMAGE_TAG=$(echo ${{ github.ref }} | sed -e "s#refs/heads/release/##g")
          IMAGE_NAME_WITH_CR="${CR_NAME}/${CR_USER,,}/${IMAGE_NAME}:${IMAGE_TAG}"
          ls -al ./
          yq eval --inplace ".spec.template.spec.containers[0].image=\"${IMAGE_NAME_WITH_CR}\"" './staging/deployment.yaml'
      - name: Create Pull-Requset
        run: |
          IMAGE_TAG=$(echo ${{ github.ref }} | sed -e "s#refs/heads/release/##g")
          git add .
          git commit -m "update docker image version ${IMAGE_TAG} in k8s manifest" || :
          git push -f --set-upstream origin test/${IMAGE_TAG}
          hub pull-request -b main -m "Release" || :
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}
